using UnityEngine;
using System.Collections;

public class fuckquaternions : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Debug.Log (transform.rotation.ToString ());
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}